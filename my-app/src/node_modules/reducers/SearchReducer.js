import { SEARCH_SUCCESS, HIDE, SETACCOUNT, CLEAR } from 'actionTypes'

const initialState = {
    results: [],
    account: {},
    visi: false
    
}

// you should separate out your reducers as much as possible to help with scaling.
// this is just an example showing how its done. see searchBox reducer for most of the reducing stuff

export default function SearchReducer(state = initialState, action) {
    switch (action.type) {
        case SEARCH_SUCCESS:
            return { ...state, results: action.results, visi: true}
        case SETACCOUNT:
            return { ...state, account: action.account}
        case HIDE:
            return { ...state, visi: false}
        case CLEAR:
            return { ...state, account: {} }
        case CHANGEPART: 
            return { ...state, data: {...data, [action.target]: action.value  }}
        default:
            return state
    }
}